# 全面审计报告 — 2026-06-19

## 审计范围
- 安全性（XSS/注入/密钥泄露）
- 正确性（逻辑 bug/边界条件/竞态）
- 性能（内存/计算瓶颈）
- 代码质量（死代码/一致性）

---

## 已修复

### P0 — Critical
| # | 问题 | 修复 |
|---|------|------|
| C-1 | What-If 功能完全不可用：`runMonteCarloUI` 从未填充 `simulationHistory`，且 rounds 已被移除 | 在 MC 循环中 push `{champion, matchResults}` 轻量数据（含 round 标签），`queryWhatIf` 改为基于 matchResults 聚合 |
| C-2 | `calibrateProbs` 全局副作用：退化检测时将 `isotonicCalibration` 设为 null，导致校准永久失效 | 删除 `isotonicCalibration = null`，退化时仅返回原始概率 |

### P1 — High
| # | 问题 | 修复 |
|---|------|------|
| S-1 | API Key 未转义注入 innerHTML value 属性（Stored XSS） | `getApiKey()` 包裹 `esc()` |
| S-2 | e.message 未转义插入 innerHTML（3 处 catch 块） | `esc(e.message)` |
| S-3 | What-If 团队名未转义 | `esc(t1)` / `esc(t2)` / `esc(winner)` |
| C-5 | `renderEVSection` 筛选后空数组导致 NaN | `filtered.length > 0 ? ... : '-'` |

### P0（此前已修复）
| # | 问题 | 修复 |
|---|------|------|
| - | Isotonic calibration 退化导致所有比赛概率趋同 | `calibrateProbs` 双重防护（已修） |
| - | What-If 平局过滤逻辑错误 | `queryWhatIf` 检查 `ga===gb`（已修） |
| - | Rankings sort ExpPts→Pts 不一致 | 统一用 Pts（已修） |
| - | `buildKOBracket` 原地 sort | `.slice().sort()`（已修） |

---

## 待修复（P1/P2）

### 正确性
| # | 问题 | 风险 | 位置 |
|---|------|------|------|
| C-3 | `fetchPolymarketResults` 使用 `ev.endDate`（市场结算日期）代替比赛日期 | P1 | L4199, L4270 |
| C-4 | `fetchPolymarketResults` 无精确比分时伪造 1-0/0-1 比分 | P1 | L4267-4268 |
| C-6 | `dynamicElo` 异步竞态：多个 async 函数并发操作全局状态 | P1 | 多处 |
| C-7 | localStorage lost-update 竞态：读-改-写模式无锁 | P1 | `loadState`/`saveState` |
| C-8 | 主市场结果覆盖 Exact Score 精确比分 | P2 | L4275-4282 |
| C-9 | i18n 缺失键 `radar.model` / `radar.market` | P2 | L3795-3804 |

### 安全
| # | 问题 | 风险 | 位置 |
|---|------|------|------|
| S-4 | API Key 明文存储于 localStorage | P1 | L3035-3043 |
| S-5 | 浏览器直接调用外部 API 暴露 Key（fallback 路径） | P1 | L3896 |
| S-6 | fetch 错误处理泄露内部 URL 结构 | P2 | L3862, L3878, L3924 |
| S-7 | API Key 无前端输入验证/长度限制 | P2 | L3849-3878 |
| S-8 | 页面加载自动发起 API 请求无确认 | P2 | L4547 |

### 性能
| # | 问题 | 影响 | 位置 |
|---|------|------|------|
| P-1 | simulationHistory 内存膨胀（~50MB for 10k sims） | 高 | L1941-1963 |
| P-2 | fetchPolymarketMatchOdds 串行 API 调用（15-30s） | 高 | L4097-4120 |
| P-3 | Tab 切换全量重渲染无缓存 | 中 | L3049-3056 |
| P-4 | renderMatches 线性搜索 actualResults | 中 | L3243 |
| P-5 | loadState() 频繁 JSON.parse（35+ 处调用） | 中 | L3036-3040 |
| P-6 | getGroupStatus 低效分组查找 | 低 | L2812-2819 |
| P-7 | calculateEV 无缓存，每次 tab 切换重算 | 中 | L2153-2203 |
| P-8 | init 序列重复执行重计算 | 中 | L4543-4547 |
